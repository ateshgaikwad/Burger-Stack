pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Start') {
            steps {
                sh 'docker compose up -d'
            }
        }

        stage('Stop') {
            steps {
                sh 'docker compose down'
            }
        }

    }
}
