pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('compose'){
            steps {
              sh "cd devops-microservices && docker-compose up --build -d"
}
}
}
}
